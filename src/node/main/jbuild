(jbuild_version 1)

(library
 ((name node_main_lib)
  (libraries (utils minutils cmdliner node_net node_shell))
  (flags (:standard -w +27-30-40@8
                    -open Error_monad
                    -open Hash
                    -open Utils
                    -open Tezos_data))
  (wrapped false)))
