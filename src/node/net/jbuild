(jbuild_version 1)

(library
 ((name node_net)
  (libraries (utils minutils conduit-lwt-unix cohttp cohttp-lwt-unix))
  (flags (:standard -w +27-30-40@8
                    -open Error_monad
                    -open Hash
                    -open Utils
                    -open Tezos_data))
  (wrapped false)))
